# Miscellaneous stuff I'm sharing for lab development work

## Dialogflow checks

Some example code of how you can check for specific tasks being completed in Dialogflow. To set up and use:

1. Clone the repo
2. Create a Python virtual environment and install the requirements
3. If you want to override the default values, set environment variables
4. Run the script

